import React from 'react'
import { Header } from '@egovernments/digit-ui-react-components';

const DownloadBill = () => {
  return (
    <React.Fragment>
         <Header className="works-header-search">{'Download Bill'}</Header>
         {/* <ResultsTable 
            config={configs?.sections?.searchResult?.uiConfig} 
            data={data} 
            isLoading={isLoading} 
            isFetching={isFetching} 
            fullConfig={configs}
        /> */}
    </React.Fragment>
  )
}

export default DownloadBill;